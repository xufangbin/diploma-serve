<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qrsx.admin.company.mapper.ArcCompanyMapper">

    <select id="selectCompany" resultType="com.qrsx.admin.company.data.ArcCompany">
        select
               *
        from
             arc_company
        <if test="fuzzy != null and fuzzy != ''">
            where
                enterprise_name like concat('%',#{fuzzy},'%')
                or social_code like concat('%',#{fuzzy},'%')
                or legal_person like concat('%',#{fuzzy},'%')
                or address like concat('%',#{fuzzy},'%')
                or linkman like concat('%',#{fuzzy},'%')
                or phone like concat('%',#{fuzzy},'%')
        </if>
        order by data_create_date_time desc
        limit #{page},#{pageSize}
    </select>
    <select id="selectTotal" resultType="java.lang.Long">
        select count(*) from arc_company
        <if test="fuzzy != null and fuzzy != ''">
            where
                enterprise_name like concat('%',#{fuzzy},'%')
                or social_code like concat('%',#{fuzzy},'%')
                or legal_person like concat('%',#{fuzzy},'%')
                or address like concat('%',#{fuzzy},'%')
                or linkman like concat('%',#{fuzzy},'%')
                or phone like concat('%',#{fuzzy},'%')
        </if>
    </select>
    <select id="selectEnterpriseName" resultType="java.util.Map">
        select social_code socialCode,enterprise_name enterpriseName from arc_company
    </select>
</mapper>