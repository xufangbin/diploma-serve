<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qrsx.admin.pubmenus.mapper.DicPubListMapper">

    <select id="selectPubList" resultType="com.qrsx.admin.pubmenus.data.DicPubList">
        select
        *
        from
        dic_publist
        <if test="fuzzy != null and fuzzy != ''">
            where
            name like concat('%',#{fuzzy},'%')
            or discription like concat('%',#{fuzzy},'%')
        </if>
        order by create_date_time desc
        limit #{page},#{pageSize}
    </select>
    <select id="selectTotal" resultType="java.lang.Long">
        select count(*) from dic_publist
        <if test="fuzzy != null and fuzzy != ''">
            where
            name like concat('%',#{fuzzy},'%')
            or discription like concat('%',#{fuzzy},'%')
        </if>
    </select>
</mapper>